import React, { Component } from 'react';
import Pt from 'prop-types';
import './Login.less';

export default class Login extends Component {
  static propTypes = {
    //Тут описываешь тип пропсов;
  };
  render() {
    return (
      <div>
        12222
      </div>
    );
  }
}
